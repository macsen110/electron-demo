import { ipcRendererSetup, callEvent } from './callEvent';
import { ipcMainSetup, registEvents } from './registEvents';

module.exports = {
  ipcRendererSetup,
  ipcMainSetup,
  callEvent,
  registEvents
};
